```markdown
# Python Week 4 - Functions

`welcome.py` contains a welcome function that creates greeting messages for different names.

`toolbox.py` contains three functions: double, is_pass, and greet, which demonstrate parameters, return values, and default parameters.

The `greet` function was the hardest to write because it uses a default value for the greeting. I had to understand how the function uses "Hello" when no other greeting is provided.
```
